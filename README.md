Instructions:
1. create a database named world in pg admmin.
2. create table named permalist with two columns id (int primary key) and task (varchar(100)).
3. replace password from your database password in the index.js
4. run npm i in the project directory.
5. run npm index.js to run the project.
6. the project should start on localhost:3000. 
